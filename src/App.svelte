<script>
  import Nav from './components/Nav.svelte';
  import Hero from './components/Hero.svelte';
  import Marquee from './components/Marquee.svelte';

  // Ticket 01 scaffold: shell (nav) + hero prove the Canvas, typeface, and
  // Caption plate tokens. The Preloader mounts with ticket 04's intro
  // timeline; without it the overlay would trap the page. Remaining sections
  // land in 02/03.
</script>

<Nav />

<main>
  <Hero />
  <Marquee />
</main>
